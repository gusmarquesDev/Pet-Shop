import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    cotainer: {
        width: '100%',
        height: 200,
        backgroundColor: '#fff',
        marginTop: 10,
        borderRadius: 7,

    },
    contentLottie: {
        height: 150,
        justifyContent: 'center',
        alignItems: 'center'

    },
    cardText: {
        textAlign: 'center',
        fontSize: 16,
        fontWeight: '500'
    }
})